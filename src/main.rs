#![allow(non_snake_case, unused)]

use rust_fullstack::app;
use dioxus::prelude::*;

// fn main() {
//     LaunchBuilder::new(app).launch();
// }



fn main() {
    // launch the web app
    dioxus_web::launch(app);
}

  